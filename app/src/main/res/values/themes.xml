<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.CurveRecyclerView" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.CurveRecyclerView" parent="Base.Theme.CurveRecyclerView" />

    <style name="DefaultNumberPickerTheme" parent="AppTheme">
        <item name="colorControlNormal">#CB76EA</item>
        <item name="backgroundColor">#DEC1E8</item>
        <item name="backgroundStacked">@drawable/divider_background</item>
    </style>

    <style name="NumberPickerTheme" parent="Theme.AppCompat.Light">
        <item name="colorAccent">@android:color/white</item>

        <!-- Edit this colorControlNormal value as you need -->
        <item name="colorControlNormal">@android:color/holo_green_dark</item>
        <item name="android:textColorPrimary">@android:color/black</item>
        <item name="android:textSize">30sp</item>
    </style>

    <style name="bottomSheetBackground" parent="Widget.Design.BottomSheet.Modal">
        <item name="android:background">@drawable/half_radius_background</item>
    </style>



    <style name="AppTheme">picker</style>

</resources>